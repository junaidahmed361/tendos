# Tendos v0.1 Release Readiness Implementation Plan

> For Hermes: execute this plan immediately task-by-task with strict verification (tests + lint + typecheck + security + pre-commit + build).

Goal: Prepare Tendos for a clean v0.1.0 packaging/release pass by clarifying vision in README and ensuring all CI-equivalent checks pass locally.

Architecture: Keep code changes minimal and low-risk for v0.1. Focus on docs/metadata quality and release pipeline reproducibility rather than adding new runtime features.

Tech stack: Python 3.10+, uv, pytest, ruff, mypy, bandit, pip-audit, pre-commit, GitHub Actions.

---

### Task 1: Baseline release checks and constraints
Objective: Confirm exact quality gates from GitHub Actions and local tooling.

Files:
- Read: `.github/workflows/ci.yml`
- Read: `.github/workflows/security.yml`
- Read: `.github/workflows/release.yml`
- Read: `.pre-commit-config.yaml`
- Read: `Makefile`

Verification:
- Produce exact list of commands to run locally to mirror CI/security/release gates.

### Task 2: README vision rewrite for "Docker for agents and harnesses"
Objective: Replace current mixed/duplicated README header content with a coherent vision and roadmap-aligned messaging.

Files:
- Modify: `README.md`

Requirements:
- Remove duplicated heading artifacts.
- Position Tendos as "Docker for AI agents and harnesses".
- Explain local-first + cloud-hosted path clearly.
- Keep quick-start and development commands accurate to current repo state.
- Keep security and trust messaging (signing, encryption, verification).

Verification:
- `uv run ruff format README.md` (or full repo format check)
- Ensure no markdown/merge-conflict artifacts remain.

### Task 3: Packaging metadata sanity for v0.1.0
Objective: Ensure Python package metadata is consistent for this repository and release intent.

Files:
- Modify: `pyproject.toml` (if needed)
- Modify: `CHANGELOG.md` (if needed)

Requirements:
- Verify version remains `0.1.0`.
- Verify project URLs point to active repository.
- Keep changelog coherent for v0.1.0 baseline.

Verification:
- `uv build`
- Confirm dist artifacts are created successfully.

### Task 4: Full CI-equivalent validation pass
Objective: Run all checks required for GitHub Actions and pre-commit before release.

Commands:
- `uv sync --all-extras`
- `uv run ruff check .`
- `uv run ruff format --check .`
- `uv run mypy src/tendos`
- `uv run bandit -r src/tendos -c pyproject.toml`
- `uv run pytest --cov=src/tendos --cov-report=xml --cov-report=term-missing`
- `uvx pip-audit --ignore-vuln CVE-2026-3219`
- `uv run pre-commit run --all-files`
- `uv build`

Verification:
- All commands exit 0.
- Coverage threshold passes.
- Build produces wheel and sdist.

### Task 5: Release readiness summary
Objective: Provide concise summary of what changed and exact pass/fail status for each gate.

Files:
- None required (summary delivered to user).

Verification:
- Include explicit results for lint, format, mypy, bandit, tests, pip-audit, pre-commit, build.
